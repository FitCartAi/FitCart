# Components

Reusable UI components belong here, grouped by product area as features are built.

Expected areas:

- `onboarding/`
- `plan/`
- `grocery/`
- `ui/` for genuinely reusable primitives

Prefer feature-local components over a large generic component library during the MVP.