# Lib

Infrastructure helpers and configuration belong here.

Planned examples:

- Supabase clients when persistence is introduced
- environment/config validation
- shared validation helpers

Business rules should stay in `src/services/`, not here.