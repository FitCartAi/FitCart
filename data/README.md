# MVP Data

This directory will hold small, clearly labeled local datasets used to validate the MVP.

The first planned dataset is curated/mock grocery pricing. It must never be presented to users as live retailer pricing.

When live or partner data is introduced later, keep the pricing service interface stable so the rest of the application does not depend on the underlying source.