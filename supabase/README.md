# Supabase

Database migrations and local Supabase project files will live here once persistence is introduced.

Supabase is planned for PostgreSQL and authentication, but both are intentionally deferred until the guest-mode vertical slice is working.

Do not store credentials, service-role keys, or exported user data in this directory.